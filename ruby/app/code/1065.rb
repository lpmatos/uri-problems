#!/usr/bin/ruby

# =============================================================================
# MAIN
# =============================================================================

if __FILE__ == $0
    valores = ->(tamanho) { Array.new(tamanho) }
    lista = (valores.call(5)).to_a
    puts "#{valores}"
end
